# 文章总结

## Introducing advanced tool use on the Claude Developer Platform
> 在 Claude 开发者平台上引入高级工具的使用
> https://www.anthropic.com/engineering/advanced-tool-use

文章总结：Claude 高级工具使用 (Advanced Tool Use)
这篇文章来自 Anthropic 工程团队，介绍了三个新的 beta 特性，用于解决 大规模工具调用场景下的效率瓶颈问题。

---
核心痛点

当工具数量多时，工具定义会消耗大量 token（生产环境中可达 5万~13万+ token），每次工具调用都需要单独的模型推理，严重影响性能和成本。

---
三大解决方案

1. Tool Search Tool（工具搜索工具）
- 不再一次性加载所有工具定义，而是让 Claude 按需动态发现相关工具
- 将工具标记为 defer_loading: true，只预加载核心高频工具
- 效果：token 消耗从 ~77K 降到 ~8.7K（减少 85%），准确率显著提升
- 适用场景：10+ 工具、超过 10K token 的多工具/多 MCP 服务器场景

2. Programmatic Tool Calling（编程式工具调用）
- Claude 编写 Python 代码来编排多个工具调用，中间结果不进入上下文窗口
- 效果：token 减少 37%，消除 19+ 次推理轮次，准确率提升
- 举例：处理 20 个团队成员的报销数据，2000+ 行中间数据不会涌入上下文，只返回最终结果
- 适用场景：大数据集处理、多步骤依赖工作流、并行操作、需要显式控制流的条件逻辑

3. Tool Use Examples（工具使用示例）
- 为工具提供具体的使用范例，弥补 JSON Schema 无法传达的使用模式
- 包括：数据格式约定、嵌套结构用法、参数关联模式、可选字段使用时机
- 效果：复杂参数处理场景准确率从 72% 提升到 90%

---
最佳实践

1. 分层策略：先解决最大瓶颈，不要同时上所有特性
2. 优化发现：工具命名和文档要清晰描述，提高搜索匹配效果
3. 文档化返回格式：在工具描述中说明返回数据结构，帮助 Claude 正确解析
4. 精心设计示例：提供 1-5 个覆盖典型和边界情况的示例

---
一句话总结

这篇文章的核心思路是：从静态的全量工具定义，转向智能的动态按需编排，通过"搜索发现 + 代码编排 + 示例引导"三板斧，大幅降低多工具场景下的 token 消耗和延迟，同时提升准确率。


## 把 320 个 Claude Code 实例拉成一支“智能体团队”！Steve Yegge：年底前，写软件不再“敲代码”，而是像玩“策略调度”游戏

核心内容

  1. AI 编程的演进历程
  - GPT 3.5 → GPT 4.0 → Sonnet 3.7/Claude Code → Opus 4.5，每一代都带来质变
  - 从"代码补全"到"聊天导向编程(CHOP)"，再到如今的"多智能体编排"

  2. Beads — 共享记忆系统
  - 本质是一个任务图 + SQL 数据库 + Git 账本的三合一系统
  - 解决大模型"健忘"问题，让智能体之间能共享记忆和上下文
  - 即将迁移到 Dolt（Git 原生数据库），解决 SQLite 合并冲突等问题

  3. Gas Town — 多智能体编排系统
  - Steve 曾同时运行 320 个 Claude Code 实例，每个约 1GB 内存
  - 给智能体设定角色（镇长、工兵等），通过类似"邮件系统"互相协作
  - 核心挑战：合并冲突、设计同步、智能体间的协调

  4. 开发者角色的转变
  - 从"写代码的人"变成"带团队的人"——产品经理 + 技术主管 + 工厂厂长
  - 重要的不再是语法和实现细节，而是理解系统、定义规范、验证质量
  - 软技能、沟通能力、全局视野变得前所未有地重要

  5. 上下文管理的两派之争
  - 最小化派：拆小任务、省 token、追求性能
  - 最大化派：喂足上下文、让模型理解"为什么"、提升战略决策质量
  - Steve 认为两者都有价值，对应不同场景

  6. 未来软件生存公式
  - 能帮大模型节省 token、缩短路径的工具会被优先选择并存活
  - 大模型能自行完成的功能，对应的软件必然消亡
  - 基础设施类产品（数据库、编排系统、消息队列等）最有生存优势

  ---
  金句摘录

  关于趋势判断：
  "唱衰 AI 的朋友，你们的视野大约只局限于过去三个月。这有点像没学过微积分的人，他们只能看到导数，却看不到斜率。"

  关于开发者转变：
  "这就让单个开发者遇到了以往只有团队才会遇到的问题。"

  "代码编写开始由智能体接管……我们自己扮演的就是工程领导的角色。"

  关于信任与效率：
  "信任度越高，用户的耐心就越低。"

  关于质量：
  "质量是种选择，就这么简单。"

  关于未来形态：
  "可能到今年年底，人们就会像玩游戏那样开发软件。"

  "智能体慢慢变得像角色扮演游戏、策略游戏乃至即时战略游戏。"

  关于软件生存：
  "要想在 AI 的冲击下生存下来，就必须成为 AI 的优先选项。"

  "只要能让大模型知晓你的工具，那项目活下来就不成问题。"

  "大模型都很懒惰，总是选择最短路径。"

  关于能力本质（引用 Peter Norvig）：
  "优秀程序员应当拥有怎样的特质？——能够把整个问题都记在脑子里。"

  关于心态：
  "智能体就像普通工程师那样，是肯定要犯错的。犯错并不值得大惊小怪，真正要关注的是那些宣称完成了任务的智能体。"

https://softwareengineeringdaily.com/2026/02/12/gas-town-beads-and-the-rise-of-agentic-development-with-steve-yegge/
https://mp.weixin.qq.com/s/VEXQvpXlu6a9OZWNo-NVMA
